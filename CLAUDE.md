# CLAUDE.md

@AGENTS.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project context

WordPress site for RytkÃ¶sten sukuseura ry (rytkoset.net). AGENTS.md contains project principles, priorities, and collaboration model â€” CLAUDE.md covers the technical environment and architecture.

## Development environment

```bash
# Start local environment
docker compose up -d
# WordPress: http://localhost:8000

# Stop
docker compose down
```

Three containers: `rytkoset-wp` (WordPress 7 / PHP 8.3), `rytkoset-db` (MariaDB), `rytkoset-joomla-db` (Joomla migration). Only `wp-content/` is mounted from host â€” file changes are reflected immediately without restart. WordPress core persists in the `wp_core` named volume.

A fourth, on-demand `wpcli` service (`wordpress:cli-php8.3`, `cli` Compose profile, does not start with `docker compose up`) runs one-off WP-CLI commands, e.g. `docker compose run --rm wpcli wp theme activate rytkoset-theme`. Use it to install/configure WordPress non-interactively.

On Windows without Docker Desktop, run Docker Engine inside WSL2 â€” see `docs/local-dev-wsl.md` for the full setup (Docker Engine install, WP-CLI bootstrap, plugin notes).

## Linting and CI

No separate build step. Three checks run in CI for every PR and `main` push (`.github/workflows/php-ci.yml`):

**1. PHP syntax validation** (`php -l`) — hard gate:

```bash
find wp-content/themes/rytkoset-theme -name "*.php" -print0 | xargs -0 -n1 php -l
```

**2. WordPress Coding Standards** (PHP_CodeSniffer + WPCS) — hard gate.

phpcs is a Composer dev dependency (`composer.json`; the only Composer use in the repo). The ruleset lives in `phpcs.xml.dist` (base `WordPress-Core` + `WordPress.Security`, targets the theme + `wp-content/maintenance.php`, excludes `assets/vendor/`, text domain `rytkoset-theme`, prefixes `rytkoset_theme` / `rytkoset`). It contains narrow exceptions for bbPress public hook names and the six underscore-containing CPT template filenames required by WordPress template hierarchy. Run locally:

```bash
composer install          # once, installs phpcs + WPCS into vendor/ (gitignored)
composer run lint         # phpcs — report violations
composer run lint:fix     # phpcbf — auto-fix whitespace/indentation
```

All files in the ruleset must pass without errors or warnings. Keep any `phpcs:ignore` directive scoped to the smallest possible line or block and include the technical reason.

> Note: phpcbf's `Generic.WhiteSpace.DisallowSpaceIndent` converts leading spaces to tabs at the configured tab width. When cleaning legacy 8-space indentation, run phpcbf with `--tab-width=8`, then verify the final result with the normal `composer run lint` command used by CI.

**3. PHPUnit unit tests** — hard gate.

phpunit is a Composer dev dependency. Config is in `phpunit.xml.dist`; tests live in `tests/`. Run locally:

```bash
composer install          # installs phpunit into vendor/ (gitignored)
composer run test         # phpunit
```

The theme has **no WordPress test install**. These are lightweight unit tests: `tests/bootstrap.php` defines just enough WordPress/WooCommerce stubs (sanitizers, an in-memory user-meta store, a controllable `current_datetime()`, and minimal `WP_User`/`WP_Post`/`WC_Order`/`WC_Product` objects) to load `inc/user-membership.php`, `inc/woocommerce-membership.php` and `inc/digital-magazine-access.php` and exercise their logic without a database. Every test extends `Rytkoset_Theme_Test_Case`, which resets the stub state in `setUp()`. Coverage targets the EPIC 10 membership decision logic: `rytkoset_theme_user_is_active_member()` (the member gate), the FI/ISO date and period sanitizers, the #302 order→membership resolution (`rytkoset_theme_resolve_order_membership`) and application rules (idempotency, never-shorten, guest/unmatched fail-closed, email-on-activation), and #420 manual magazine grants/revocation + the OR-logic access filter. Admin render/save callbacks (nonces, `$_POST`, capabilities) are intentionally out of scope. When adding membership logic, prefer extracting the decision into a pure/near-pure function and adding a test next to the existing ones. The stubs are deliberately minimal — extend `tests/bootstrap.php` only with what a new test needs. PHP-version note: CI runs PHPUnit on PHP 8.3; locally without PHP installed you can run it through Docker (`docker run --rm --entrypoint php -v "$PWD":/app -w /app composer:2 vendor/bin/phpunit`).

## Deploy

- `dev` branch â†’ automatic FTPS deploy â†’ `dev.rytkoset.net` (when changes in `wp-content/themes/rytkoset-theme/**`)
- `main` branch â†’ no automatic deploy
- Production (`rytkoset.net`) deploy is manual via `.github/workflows/deploy-production.yml`, defaulting to `main` and requiring production FTPS secrets (`PROD_FTP_HOST`, `PROD_FTP_USERNAME`, `PROD_FTP_PASSWORD`, `PROD_FTP_PORT`)

## Commit messages

Conventional Commits (see `CONTRIBUTING.md`):

```
feat(events): add single event template
fix(woo): fix membership order save
docs: update README with staging instructions
refactor: split functions.php into inc/ modules
```

Do not create commits automatically â€” report the implementation first, suggest a commit message, and let the user review the diff before committing.

## Theme architecture

The theme `wp-content/themes/rytkoset-theme/` is the only versioned codebase. WordPress core and plugins are not in the repo.

`wp-content/maintenance.php` overrides WordPress's built-in maintenance page (shown when a `.maintenance` file exists in the WordPress root, or during core/plugin updates). It is a self-contained PHP/HTML file â€” all CSS is inline, fonts are self-hosted (#379: Manrope + Newsreader woff2 in `wp-content/themes/rytkoset-theme/assets/fonts/`, declared as `@font-face` in the inline CSS â€” no `fonts.googleapis.com`/`fonts.gstatic.com` requests, GDPR), and theme assets (incl. fonts) are referenced via `WP_CONTENT_URL`. It reads Customizer values (`rytkoset_theme_maintenance_concept`, `rytkoset_theme_maintenance_return_text`, `rytkoset_theme_contact_email`, `custom_logo`) plus `blogname`/`home`/`siteurl`. **Important:** WordPress runs `wp_maintenance()` very early (`wp-settings.php` line ~79), *before* `option.php`, `$wpdb`, `formatting.php` and the theme/options APIs load — so `get_option()`, `get_theme_mod()`, `esc_*()`, `status_header()` etc. are **not** available. The file therefore: (1) sends `503` + `Retry-After` + no-cache headers itself (`status_header`/`nocache_headers` with raw `header()` fallbacks, #374); (2) defines lightweight `esc_html`/`esc_attr`/`esc_url` fallbacks; (3) opens a DB connection via `require_wp_db()` and reads the needed options + theme mods (`theme_mods_<stylesheet>`, unserialized) directly with `$wpdb`, building the logo URL from `_wp_attached_file` + uploads base URL (#382). Loading `option.php`/`formatting.php` early was rejected: each real `esc_*`/`get_option` call pulls a long dependency chain (`esc_sql` → `kses.php` → UTF-8 helpers) that fatals in the partially-loaded state. Customizer settings are under **Huoltotila** in the WordPress Customizer.

### Template hierarchy

| File                         | Purpose                                             |
| ---------------------------- | --------------------------------------------------- |
| `front-page.php`             | Front page                                          |
| `page.php`                   | Static pages                                        |
| `page-blogi.php`             | Blog landing page and all published posts           |
| `category.php`               | Blog category archive using the main category query |
| `single.php`                 | Blog post                                           |
| `single-rytkoset_event.php`  | Single event                                        |
| `single-gallery_album.php`   | Single album                                        |
| `archive-rytkoset_event.php` | Event archive (`/tapahtumat`)                       |
| `archive-gallery_album.php`  | Album archive (`/albumit`)                          |
| `header.php` / `footer.php`  | Site header; footer = pre-footer band + slim footer |

Front page (#289): `front-page.php` builds a Claude Design layout â€” a split hero (`.hero__content--split`) with a welcome illustration, a Sukujuhlat **feature** band as the showpiece (date/location chips + floating badge), then alternating light/dark content bands (Albumit, JÃ¤senyys, Kauppa, Sukututkimus/Viljo). Band tones use the `--home-band-*` tokens (`assets/css/base.css`) which adapt to the dark theme; styles live in `assets/css/home.css`. Illustrations are theme assets under `assets/images/home/`.

Footer (Footer C, #278): `footer.php` renders a pre-footer newsletter band above the slim footer on every page â€” `template-parts/pre-footer-large.php` on the front page (`is_front_page()`), `template-parts/pre-footer-compact.php` elsewhere. Each partial calls `rytkoset_theme_get_footer_newsletter_form()` and renders nothing when there is no form to show (active subscriber, or newsletter shortcode not configured). The slim footer (`<footer class="site-footer">`) holds brand, footer nav, contact email and social links, and shows on all pages.

### functions.php and inc/ modules

`functions.php` (~580 lines) contains theme setup, asset enqueue, header/nav helpers, and shared WooCommerce helpers (`get_order_from_admin_screen_object`, `get_supported_order_statuses`). Domain-specific logic is split into modules under `inc/`:

| File                                   | Contents                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `inc/security.php`                     | Security hardening: blocks user enumeration (REST `/wp/v2/users` logged-out, `?author=N` redirects, core users sitemap, generic credential errors on `wp-login.php` via `wp_login_errors`), disables XML-RPC (`xmlrpc_enabled`, pingback methods, `X-Pingback` header), sends frontend security headers (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy` via `send_headers`; HSTS/CSP left to server), and blocks spam registrations (honeypot field + blocked email domains via `registration_errors`). Toggles `rytkoset_theme_enable_security_hardening` / `rytkoset_theme_disable_xmlrpc`; filterable via `rytkoset_theme_security_headers` and `rytkoset_theme_blocked_registration_email_patterns`                                                                                                                                                                                                                                                                                                                                                                                                      |
| `inc/events.php`                       | Event CPT, meta field registration and getters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `inc/event-registrations.php`          | Free event registration CPT and form                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `inc/event-registration-privacy.php`   | Privacy Tools export, erasure and anonymization for free event registrations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `inc/event-participants-admin.php`     | `Events > Participants` admin view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `inc/event-participants-messaging.php` | `Events > Messaging` bulk email queue and WP-Cron rate limiter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `inc/event-roles.php`                  | `event_organizer` role and capabilities. Also a profile-screen **secondary-role** toggle (#423): a "Tapahtumien järjestäjä" checkbox (`edit_users` only, nonce-protected) adds/removes the `event_organizer` role via `WP_User::add_role()`/`remove_role()` without touching the user's primary role, so e.g. Päätoimittaja + Event Organizer can coexist. Save runs on `profile_update` (not `edit_user_profile_update`) so it fires after WordPress's role-dropdown `set_role()` replaces the role set, otherwise the dropdown would wipe the added role. Documented in `docs/events.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `inc/gallery-albums.php`               | Gallery Album CPT and gallery stack logic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `inc/media-library.php`                | Media library ordering by album                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `inc/digital-magazines.php`            | Digital magazine CPT and access checks: hierarchical `digital_magazine` content (top-level magazine + child articles), access mode meta `_rytkoset_magazine_access_mode` (`free`, `members_only`, `member_and_regular`, `paid`), shared gate helper `rytkoset_theme_user_can_read_digital_magazine()` that normalizes child articles to the parent magazine, admin metabox for top-level magazines, and archive excerpt protection for restricted magazines. Purchase checks are a #201 extension point via `rytkoset_theme_user_has_purchased_digital_magazine` filter; #381 handles REST/feed/search/oEmbed leakage beyond the template gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `inc/digital-magazine-access.php`      | Manual digital magazine access grants (#420, Model A): per-user grants stored in user meta `rytkoset_magazine_access` (magazine ID → grant timestamp, manual grants only — WooCommerce purchases are resolved live by #201, never copied here). "Digilehtien käyttöoikeudet" profile section (`edit_users`, nonce + grantable allowlist) lists `paid`/`member_and_regular` magazines with a per-magazine checkbox. A callback on the `rytkoset_theme_user_has_purchased_digital_magazine` filter merges manual grants with #201's live purchase lookup via OR-logic. Shared access-email helper `rytkoset_theme_send_digital_magazine_access_email()` (also called by #201 on purchase) sends a one-time notice on a no-access→access transition; revocation is silent. WooCommerce-independent                                                                                                                                                                                                                                                                                                                                                    |
| `inc/email.php`                        | Default mail sender (`RytkÃ¶sten sukuseura ry` / `rytkoset_theme_get_contact_email()`) for theme-sent `wp_mail()`; overrides only WordPress's default sender via `wp_mail_from`/`wp_mail_from_name`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `inc/icons.php`                        | Inline SVG icon helper (`rytkoset_theme_inline_icon`) â€” reads `currentColor` glyphs from `assets/icons/{social,ui}/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `inc/share.php`                        | Share buttons (Facebook, X, WhatsApp); icons via `rytkoset_theme_inline_icon`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `inc/social-links.php`                 | Social media links in header/footer; icons inlined via `rytkoset_theme_inline_icon`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `inc/attachment-iptc.php`              | IPTC headline and description sync for attachment images                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `inc/seo-meta.php`                     | Open Graph and Twitter Card meta tags. Also holds the restricted-content leak guard for the SEO/social description routes: shared `rytkoset_theme_content_is_access_restricted()` (ORs `should_hide_digital_magazine_content` #381 + `should_hide_members_only_page` #392) drives both the theme's own OG/Twitter description and the Rank Math plugin output — `rank_math/frontend/description` (covers meta/OG/Twitter in one value) and `rank_math/json_ld` (blanks node descriptions). Manual excerpt kept as teaser, else falls back to the site tagline so protected `post_content` never reaches the page HTML                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `inc/login.php`                        | `wp-login.php` redesign: JS builds a split-layout brand panel + form card around `#login`, per-view copy/tabs, theme-following dark mode, Finnish translations. The password reset/set screen (`action=rp`) presents an empty masked `type=password` field (WordPress's auto-generated plaintext password + Generate button removed via JS) so password managers recognise it; the reveal-icon CSS outweighs WordPress's own `.wp-hide-pw` rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `inc/newsletter.php`                   | AcyMailing newsletter integration: footer signup, subscription helpers and opt-in hooks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `inc/user-membership.php`              | User membership status on the profile screen (Jäsenyys section): type (none/annual/family/lifetime) + period + expiry stored in user meta (`rytkoset_membership_*`, not a role); shared active-membership helper `rytkoset_theme_user_is_active_member()` (lifetime always active, annual/family active until expiry, fail-closed otherwise). Source of truth for member-gated content and digital magazine access (EPIC 9/10). Sends a Finnish plain-text confirmation email on a non-active→active transition via shared helper `rytkoset_theme_send_membership_confirmation_email()` (#390; caller detects the transition — profile save via `$was_active`, #302 on the order path); sender from `inc/email.php`                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `inc/members-only-pages.php`           | Members-only content pages (#392, EPIC 10, first member benefit). Per-page flag in post meta `_rytkoset_members_only` (`yes`), set from a "Näkyvyys" metabox on the page editor (`edit_post`, nonce + checkbox). Shared gate `rytkoset_theme_user_can_read_members_only_page()` = active member (`rytkoset_theme_user_is_active_member()`, #301) OR can-edit-page (editor preview). Template gate lives in `page.php` (title always rendered, content replaced with `template-parts/members-only-locked.php` login/join CTA pointing at `/sukuseura/jasenyys`). #381-style leak guards via `rytkoset_theme_should_hide_members_only_page()`: blanks `rest_prepare_page` content/excerpt, replaces `get_the_excerpt` (search/oEmbed/summary feed) and `the_content_feed`; the SEO/social description + Rank Math leak routes are handled in `inc/seo-meta.php` (see that row); manual excerpt kept as teaser. Lock card styles `.members-only-lock` live in `components.css` (site-wide). Pages only; WooCommerce-independent. Page-cache bypass requirement documented in `docs/jasenille-rajatut-sivut.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `inc/woocommerce-mollie.php`           | Mollie Finnish translations, RF reference normalization                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `inc/woocommerce-membership.php`       | Membership products, checkout notice, admin column and metabox. Structured member checkout fields (#391): when the cart has a names-required membership product (`_rytkoset_member_names_required = yes`), the Block checkout shows `rytkoset/member_X_name` + `rytkoset/member_X_email` fields (X = 1–6) registered via `woocommerce_register_additional_checkout_field` (`location = order`). The theme publishes the visible row count on the Store API `cart.extensions.rytkoset_membership.member_row_count` (1 for individual/lifetime, 6 for `annual_family`, 0 otherwise) used by conditional required/hidden JSON schema — same model as Tampere 2026. Row 1 name + email required; family extra rows optional; email format validated via `validate_callback`. Stored as `_wc_other/...` order meta; empty rows cleaned up and hidden from confirmation/email/admin; metabox lists entered members with fallback to the customer note for old orders. Automatic membership update (#302): on `woocommerce_order_status_processing`/`_completed`, a paid membership order updates the buyer's user membership meta (`inc/user-membership.php` model) — product type → user type, expiry from the product's `_rytkoset_membership_expiry_date` field (admin-set "Jäsenyys voimassa asti", e.g. next family-meeting date), reusing `rytkoset_theme_send_membership_confirmation_email()` (#390). Idempotent via `_rytkoset_membership_order_processed` order meta; never shortens an existing membership; guest/unmatched/missing-type/missing-expiry cases leave an admin order note (fail closed) |
| `inc/woocommerce-digital-magazine.php` | Digital magazine WooCommerce product linking + member pricing (#201). Two-product model (#199): magazine post meta `_rytkoset_magazine_regular_product_id` (paid + member_and_regular) and `_rytkoset_magazine_member_product_id` (member_and_regular only); admin "Maksutuotteet" metabox on top-level magazines (nonce/cap/publish-status validation). `..._purchase_product()`/`..._purchase_cta()` pick member vs regular product by `rytkoset_theme_user_is_active_member()` (fail-safe to regular). Purchase detection is a second callback on the `rytkoset_theme_user_has_purchased_digital_magazine` filter via `wc_customer_bought_product()` (completed/processing only). Member-price product blocked without active membership (`woocommerce_add_to_cart_validation` + `woocommerce_check_cart_items`). On `woocommerce_order_status_completed`/`_processing`, sends #420's shared access email once per order+magazine (order meta `_rytkoset_magazine_access_email_sent`, static re-entrancy guard); guest orders skipped. Magazine-side product links are not synced by the product-sync tool (product IDs differ per environment) |
| `inc/woocommerce-member-coupon.php`    | Membership-bound discount coupon (#393, EPIC 10). Adds a "Vain jäsenille" checkbox to the WooCommerce coupon *Usage restriction* panel (coupon meta `_rytkoset_member_coupon = yes`, saved via `woocommerce_coupon_options_save`). Validation callback on `woocommerce_coupon_is_valid` throws a Finnish error unless the customer is a logged-in active member (`rytkoset_theme_user_is_active_member()`, #301) — works in cart, classic checkout and Checkout Block / Store API since all route through `WC_Discounts`. Product restrictions, discount % and validity stay on WooCommerce's own coupon settings; no auto-apply, no per-product member prices. Member coupon code is meant to be shared on a members-only page (#392). Documented in `docs/woocommerce-jasenalennus.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `inc/woocommerce-tampere-2026.php`     | Tampere 2026 participation fee: product, checkout fields, admin, organizer notifications                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `inc/woocommerce-bus-transport.php`    | Tampere 2026 bus transport add-on product (#442): standalone, independently purchasable variable product (SKU `tampere-2026-bussikyyti`) where each variation is a pickup point (`Lähtöpaikka` attribute) and the whole bus capacity is shared at parent level (`Manage stock` on the parent, `Stock quantity` = seats). One variation unit = one passenger; same model as the Tampere 2026 adult/child fee. Cross-sell on the Tampere 2026 fee so it shows in the cart. Deadline + capacity gate (default `2026-07-30`, meta `_rytkoset_registration_deadline`) reuses the Tampere messages (`Ilmoittautuminen on päättynyt.` / `…on täynnä.`) and `rytkoset_theme_normalize_registration_deadline_date()`, wired via `woocommerce_is_purchasable`/`_get_availability_text`/`_single_product_summary`/`_add_to_cart_validation`/`_check_cart_items` + own inventory-tab deadline field. Bus is identified by its **own SKU** and intentionally does **not** match `rytkoset_theme_is_tampere_2026_registration_product()`, so Tampere participant checkout fields and the participant admin column never activate for it. Minimum-passenger "trip runs if enough sign up" threshold + refund handled operationally (no native WooCommerce threshold); stated in the product description. Documented in `docs/woocommerce-tampere-2026-bussikyyti.md` |
| `inc/woocommerce-product-sync.php`     | WooCommerce product sync tool for local <-> dev. Whitelisted `_rytkoset_*` meta keys sync via `rytkoset_theme_product_sync_get_meta_keys()` (filter `rytkoset_theme_product_sync_meta_keys`), incl. `_rytkoset_membership_expiry_date` (#302, added in #407). Membership-meta validation (#407): shared pure `rytkoset_theme_product_sync_validate_membership_meta()` (allowed types from `rytkoset_theme_get_membership_type_options()`, never derived from product name) requires type ∈ {annual_individual, annual_family, lifetime} when `_rytkoset_membership_product = yes`, requires period for annual but not lifetime, and flags type/period set without the product flag — export blocks (per-product WP_Error), import preview marks the product `error` (locked) and import apply rejects it too; `rytkoset_theme_product_sync_get_membership_summary()` renders readable settings in the preview. Magazine↔product links are intentionally not synced (product IDs differ per environment)                                                                                                                                                                                                                                                                                 |
| `inc/woocommerce-shop-categories.php`  | Shop category bar: lightweight category link row above the product loop on shop + product category archives (`woocommerce_before_shop_loop`); styles in `shop.css` (`.rytkoset-shop-cats`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `inc/customizer-contact.php`           | Customizer contact fields for footer and admin email; maintenance mode concept and return-text settings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

All functions use `if ( ! function_exists('rytkoset_theme_...') )` guard and `rytkoset_theme_` prefix.

### CSS structure

All local theme CSS/JS assets, including login assets and vendored PhotoSwipe files, use `rytkoset_theme_get_asset_version()` for `filemtime()`-based cache busting with the theme version as a missing-file fallback.

No build step. `style.css` holds only the theme header â€” the core CSS modules are enqueued separately in `functions.php` (`rytkoset_theme_scripts`) via a `$css_modules` loop that chains each module's `deps` to the previous one, preserving cascade order (base â†’ layout/components â†’ â€¦ â†’ responsive last). Page-specific styles (shop, forum, digital-magazine, gallery) depend on the last core module so they load after it. Cache-busted with `filemtime()`. The modules in cascade order:

```
# Core modules, loaded site-wide in cascade order:
assets/css/base.css          # Typography, color variables, base elements
assets/css/layout.css        # Containers, grids, sections
assets/css/hero.css          # Front page hero section (split layout with illustration)
assets/css/home.css          # Front page content bands (alternating light/dark, feature + story)
assets/css/404.css           # 404 page
assets/css/components.css    # Buttons, cards, general components
assets/css/nav.base.css      # Shared navigation styles
assets/css/nav.desktop.css   # Desktop navigation
assets/css/nav.account.css   # User/account menu
assets/css/nav.mobile.css    # Mobile navigation (hamburger)
assets/css/footer.css        # Footer
assets/css/responsive.css    # Media queries (last)

# Page-specific modules, enqueued conditionally:
assets/css/shop.css          # WooCommerce shop/cart/checkout
assets/css/forum.css         # bbPress forum
assets/css/digital-magazine.css  # Digital magazine pages
assets/css/gallery.css       # Gallery and albums
assets/css/login.css         # WP login page branding (enqueued in inc/login.php)
```

Use CSS variables for colors and spacing. No Bootstrap dependency.

### JavaScript

`assets/js/main.js` â€” mobile menu toggle and other general interactions.
`assets/js/photoswipe-init.js` â€” PhotoSwipe 5 lightbox initialization for album pages.
`assets/vendor/photoswipe/` â€” PhotoSwipe 5 vendored (no npm/bundler).

## Custom Post Types

### Event (`event`)

- Slug: `/tapahtumat/`
- Registered in: `inc/events.php`
- Meta keys (via `rytkoset_theme_get_event_details_meta_keys()`):
  - `_rytkoset_event_date` â€” date `YYYY-MM-DD`
  - `_rytkoset_event_start_time` â€” start time `HH:MM`
  - `_rytkoset_event_end_time` â€” end time `HH:MM`
  - `_rytkoset_event_location` â€” location
  - `_rytkoset_event_fee_type` â€” `free` | `paid`
  - `_rytkoset_event_price_text` â€” price text for display
  - `_rytkoset_event_registration_deadline` â€” free event registration deadline `YYYY-MM-DD`; empty falls back to event date for the public form cutoff
  - `_rytkoset_event_product_id` â€” linked WooCommerce product for paid registration/payment
  - `_rytkoset_event_organizer_notification_recipients` â€” event-specific organizer notification email recipients for paid event orders

Free event registration forms close after the event registration deadline. Paid event pages read the deadline and availability state from the linked WooCommerce product instead of duplicating that data on the event.

`Events > Messaging` keeps event participant messaging in WordPress. The admin form queues messages in `rytkoset_event_messaging_queue`; WP-Cron hook `rytkoset_process_event_messaging_queue` sends queued recipients with a rolling 18 `wp_mail()` attempts / 60 minutes limit tracked in `rytkoset_event_messaging_send_attempts`.

### Gallery Album (`gallery_album`)

- Slug: `/albumit/`
- Registered in: `inc/gallery-albums.php`
- Images are WordPress media attachments with `post_parent = album_post_id`
- Order: by filename (sorted in `inc/media-library.php`)

## WooCommerce integration

WooCommerce-specific logic lives in `inc/woocommerce-*.php` modules, with small shared helpers still in `functions.php`. Key areas:

- **Membership products** â€” annual and lifetime membership; checkout notice when product is in cart
- **Paid event fees / Tampere 2026** â€” linked event products, Tampere-specific checkout fields whose count comes from the `rytkoset_tampere_2026` Store API cart extension, stale participant field hiding in admin/emails, participant list in admin, CSV export, event-specific organizer email notifications
- **Mollie payments** â€” Finnish language texts, output buffering on `thankyou` page for bank transfer instructions
- **PhotoSwipe conflict** â€” WooCommerce registers PhotoSwipe 4 scripts; theme actively dequeues them to avoid conflicts

## Navigation menus

WordPress admin-managed menus: `primary` (main menu), `footer`, `account` (user/account).

## Documentation

`docs/` contains setup and maintenance guides for WooCommerce features. Read the relevant doc before making WooCommerce changes.

`docs/newsletter.md` documents the AcyMailing footer signup setup, target list, opt-in workflows and newsletter MVP boundaries.

`docs/design-system.md` documents the theme's color tokens, radius/shadow/transition variables, layout, and component conventions. Read it before writing or modifying CSS.

`docs/media-saavutettavuus.md` documents admin-facing media accessibility rules: alt-text guidance, the gallery alt-fallback chain (`rytkoset_theme_get_gallery_image_alt()`), automatic iframe titles for album videos, and PhotoSwipe keyboard shortcuts.

`docs/woocommerce-saavutettavuus.md` is a developer-facing audit of the WooCommerce-related a11y surface: cart link aria-label, the custom listbox sort widget, custom quantity controls, Tampere 2026 / membership checkout notices, and the third-party boundaries (Mollie hosted page, WC Checkout Block).

`docs/tietoturva.md` documents the theme's security hardening (`inc/security.php`: user enumeration blocking, XML-RPC disabling, frontend security headers) and the server/ops-level checklist (2FA, login throttling, updates, backups, HSTS/CSP, uploads protection). Read it before changing `inc/security.php`.
